diff --git a/deploy/alt-linux/backup/alt_deploy_backup/bundle_management.py b/deploy/alt-linux/backup/alt_deploy_backup/bundle_management.py
index 69f8e2b..afcde83 100644
--- a/deploy/alt-linux/backup/alt_deploy_backup/bundle_management.py
+++ b/deploy/alt-linux/backup/alt_deploy_backup/bundle_management.py
@@ -428,6 +428,7 @@ class BundleManager:
             if not isinstance(payload, dict):
                 return True
             if payload.get("backup_id") == backup_id and payload.get("phase") not in {
+                "aborted",
                 "committed",
                 "rolled_back",
             }:
diff --git a/deploy/alt-linux/backup/alt_deploy_backup/cli.py b/deploy/alt-linux/backup/alt_deploy_backup/cli.py
index ea37f58..da2181e 100644
--- a/deploy/alt-linux/backup/alt_deploy_backup/cli.py
+++ b/deploy/alt-linux/backup/alt_deploy_backup/cli.py
@@ -19,6 +19,7 @@ COMMANDS_WITH_ID = {
     "rehearse",
     "rehearse-status",
     "restore",
+    "recover",
     "delete",
 }
 
@@ -131,6 +132,18 @@ def _dispatch(
             "rollback_performed": result.rollback_performed,
             "cleanup_complete": result.cleanup_complete,
         }
+    if command == "recover":
+        result = RestoreService(repository).recover(backup_id)
+        return {
+            "status": "ok",
+            "result": "backup_recovered",
+            "restore_id": backup_id,
+            "backup_id": result.backup_id,
+            "phase": result.phase,
+            "services_restored": result.services_restored,
+            "rollback_performed": result.rollback_performed,
+            "cleanup_complete": result.cleanup_complete,
+        }
     if command == "delete":
         result = repository.delete(backup_id)
         return {
diff --git a/deploy/alt-linux/backup/alt_deploy_backup/restore_journal.py b/deploy/alt-linux/backup/alt_deploy_backup/restore_journal.py
index d3ddef3..bdb3aca 100644
--- a/deploy/alt-linux/backup/alt_deploy_backup/restore_journal.py
+++ b/deploy/alt-linux/backup/alt_deploy_backup/restore_journal.py
@@ -22,15 +22,19 @@ RESTORE_ID_RE = __import__("re").compile(
 _FORWARD = {
     "prepared": "staged",
     "staged": "services_stopped",
-    "services_stopped": "originals_moved",
+    "services_stopped": "originals_moving",
+    "originals_moving": "originals_moved",
     "originals_moved": "installed",
     "installed": "daemon_reloaded",
     "daemon_reloaded": "health_checked",
     "health_checked": "committed",
 }
+_PRE_MUTATION = frozenset(
+    {"prepared", "staged", "services_stopped"}
+)
 _AFTER_MUTATION = frozenset(
     {
-        "services_stopped",
+        "originals_moving",
         "originals_moved",
         "installed",
         "daemon_reloaded",
@@ -38,10 +42,19 @@ _AFTER_MUTATION = frozenset(
     }
 )
 _TERMINAL = frozenset(
-    {"committed", "rolled_back", "manual_recovery_required"}
+    {
+        "aborted",
+        "committed",
+        "rolled_back",
+        "manual_recovery_required",
+    }
 )
 
 
+def terminal_phases() -> frozenset[str]:
+    return _TERMINAL
+
+
 def _error(message: str) -> BackupError:
     return BackupError(
         code="restore_staging_failed",
@@ -272,8 +285,12 @@ class RestoreJournal:
         if self.phase != expected:
             raise _error("Restore journal phase changed unexpectedly")
         allowed = _FORWARD.get(expected) == target
-        if target in {"rolled_back", "manual_recovery_required"}:
+        if target == "aborted":
+            allowed = expected in _PRE_MUTATION
+        elif target == "rolled_back":
             allowed = expected in _AFTER_MUTATION
+        elif target == "manual_recovery_required":
+            allowed = expected not in _TERMINAL
         if not allowed:
             raise _error("Restore journal phase transition is invalid")
         safe = _safe_evidence(evidence)
@@ -283,3 +300,13 @@ class RestoreJournal:
         self.updated_at = _utc_now()
         self.evidence[target] = safe
         self._write()
+
+    def record_phase(self, evidence: Mapping[str, object]) -> None:
+        if self.phase in _TERMINAL:
+            raise _error("Restore terminal journal cannot be updated")
+        safe = _safe_evidence(evidence)
+        if not isinstance(safe, dict):
+            raise _error("Restore journal phase evidence is invalid")
+        self.updated_at = _utc_now()
+        self.evidence[self.phase] = safe
+        self._write()
