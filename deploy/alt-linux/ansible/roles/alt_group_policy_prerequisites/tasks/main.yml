---
- name: Install the ALT Group Policy client before domain join
  ansible.builtin.include_role:
    name: alt_resilience
    tasks_from: install_packages.yml
  vars:
    alt_resilience_package_names: "{{ alt_group_policy_prerequisite_packages }}"
  when: alt_group_policy_prerequisite_packages | length > 0
