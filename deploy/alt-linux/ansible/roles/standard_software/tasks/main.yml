---
- name: Install approved standard workstation packages
  ansible.builtin.include_role:
    name: alt_resilience
    tasks_from: install_packages.yml
  vars:
    alt_resilience_package_names: "{{ standard_packages + organization_packages }}"
  when: (standard_packages + organization_packages) | length > 0
