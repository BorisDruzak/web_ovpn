# Sanitised renderer snapshots

This directory contains only derived checksums from the synthetic PR2 fixture.
It contains no inventory, personal metadata, token, active password hash, or
production ISO hash.  The renderer test compares deterministic output against
the checksum list, never against a secret-bearing generated Scheme file.
