[Unit]
Description=Network Observer collection

[Service]
Type=oneshot
User=netctl
Group=netctl
AmbientCapabilities=CAP_NET_RAW
NoNewPrivileges=true
PrivateTmp=true
ProtectHome=true
ExecStart=/usr/local/sbin/netctl --json collect all --reconcile
