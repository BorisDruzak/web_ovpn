[Unit]
Description=Reconcile netctl state after collection failures
After=netctl-collect.service

[Service]
Type=oneshot
User=netctl
Group=netctl
NoNewPrivileges=true
PrivateTmp=true
ProtectHome=true
ExecStart=/usr/local/sbin/netctl --json reconcile
