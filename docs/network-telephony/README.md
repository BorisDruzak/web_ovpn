# Телефония и VPN

Дата фиксации: 2026-07-06.

Текущая проверка VPN-доступа: [findings-2026-07-06.md](findings-2026-07-06.md).

Удаленный hEX с сетью `192.168.99.0/24`: [remote-hex-192-168-99.md](remote-hex-192-168-99.md).

## Узлы и сегменты

- Центральная IP-АТС: Grandstream UCM6302.
- LAN UCM: `192.168.0.221/24`.
- WAN UCM: `192.168.5.2/24`, шлюз `192.168.5.1`.
- Фактический телефонный сегмент: `192.168.0.0/24`.
- MikroTik в телефонном сегменте: `192.168.0.250/24`, комментарий ранее определен как `Old phone network`.

## SIP-транки оператора

- `3514492600` -> SIP peer `212.220.80.52`.
- `3514492601` -> SIP peer `212.220.80.52`.
- `3514492602` -> SIP peer `212.220.80.52`.

## Входящие маршруты

- `+73514492600` -> `IVR_Day_01`, нерабочее время -> `IVR_Night`.
- `+73514492601` -> `IVR_Day_02`, нерабочее время -> `IVR_Night`.
- `+73514492602` -> `IVR_Day_03`, нерабочее время -> `IVR_Night`.
- Аналоговый trunk `351-44-9-03-19` / `FXO1` -> `Queue04-01`, нерабочее время -> `IVR_Night`.
- Аналоговый trunk `351-44-9-02-52` / `FXO2` -> `Queue05-01`, нерабочее время -> `IVR_Night`.
- `HT813_73514490324` -> внутренний `120`; trunk подозрительный или устаревший.

## FXO

На UCM настроены две внешние аналоговые линии:

- `FXO1` -> `351-44-9-03-19`.
- `FXO2` -> `351-44-9-02-52`.

Ранее они были `abnormal/red`. Нужно физически проверить медь обычным аналоговым телефоном напрямую на RJ11-кабеле, который идет в FXO.

## FXS и аналоговые внутренние телефоны

`HT818 192.168.0.222`:

- `105` зарегистрирован.
- `106` зарегистрирован.
- `107` зарегистрирован.
- `109` не зарегистрирован.
- `112` зарегистрирован.
- `113` зарегистрирован.
- `115` не зарегистрирован.
- `116` зарегистрирован, похоже факс.

`HT814 192.168.0.239`:

- `121` зарегистрирован.
- `122` не зарегистрирован.
- `123` зарегистрирован.
- `124` не зарегистрирован или порт отключен.

Важно: Panasonic KX-TG5511RU не IP-телефон и не внешняя медная линия. Это аналоговый/DECT-телефон на FXS-порту HT814/HT818. Для UCM он выглядит как обычный внутренний номер. IP-телефоны и Panasonic должны звонить друг другу короткими внутренними номерами, например `101 -> 105`, `105 -> 101`. Набор `8351...` или `+7351...` уходит во внешний вызов.

## IVR

- `7000` `IVR_Day_01`.
- `7001` `IVR_Night`.
- `7002` `IVR_Busy_107`.
- `7003` `IVR_Day_02`.
- `7004` `IVR_Day_03`.

В IVR разрешен набор других добавочных номеров, набор внешней линии запрещен. Точная карта кнопок IVR пока не снята.

## Очереди

- `6500` `Queue01` -> `Route01-01`.
- `6501` `Queue02` -> `Route02-01`.
- `6502` `Queue03` -> `Route03-01`.
- `6503` `Queue04-01` -> `101`.
- `6504` `Queue05-01` -> `101`.
- `6505` `Queue04-02` -> `Route04-01`.
- `6506` `Queue05-02` -> `Route05-01`.
- `6507` `Queue_LoopBack` -> `Route01-01`, `Route02-01`, `Route03-01`, `Route04-01`, `Route05-01`.

## Группы номеров

- `Route01-01` -> `101`.
- `Route02-01` -> `101`, `111`.
- `Route03-01` -> `101`, `108`.
- `Route04-01` -> `101`, `115`.
- `Route05-01` -> `101`, `102`.

## Исходящие

Основной исходящий trunk: `3514492600`.

Маршруты:

- `Country_CountryMobiles`, уровень `Международный`.
- `ChelObl_ChelOblMobiles`, уровень `Национальный`.
- `Sosnovka`, уровень `Локальный`.
- `Mobiles_Office`, уровень `Страна`.
- `Emergency`, уровень `Страна`.
- `LoopBack` / `LoopBack_Full` через trunk `Sosnovka` (`475 - LoopBack`).

Проблема "нет разрешения на доступ к этому номеру" решалась через права/уровень привилегий. Важно учитывать порядок исходящих маршрутов: UCM применяет первый подходящий сверху вниз.

## SIP NAT

В UCM добавлены локальные сети:

- `172.16.100.0/24`.
- `192.168.0.0/24`.
- `192.168.5.0/24`.

Внешний узел: `178.46.163.28`.

Порты:

- SIP UDP/TCP: `5060`.
- SIP TLS: `5061`.

Если подключать второе здание через VPN, подсеть второго здания нужно добавить в `SIP NAT -> Локальный сетевой адрес`.
