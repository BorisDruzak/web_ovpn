# ALT operational reliability test support

This package is test-only. It provides isolated controller paths, synthetic payloads, JSON CLI invocation and the catalog of proven OR-1 outcomes.

It must not read controller runtime state, active Vault files, SSH private keys or the accepted reference VM.
